    <footer class="mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4">
                    <h5><i class="bi bi-water"></i> Pool N Play</h5>
                    <% if (typeof settings !== 'undefined' && settings.companyAddress) { %>
                        <p><i class="bi bi-geo-alt"></i> <%= settings.companyAddress %></p>
                    <% } %>
                    <% if (typeof settings !== 'undefined' && settings.companyPhone) { %>
                        <p><i class="bi bi-telephone"></i> <%= settings.companyPhone %></p>
                    <% } %>
                    <% if (typeof settings !== 'undefined' && settings.companyEmail) { %>
                        <p><i class="bi bi-envelope"></i> <%= settings.companyEmail %></p>
                    <% } %>
                </div>
                <div class="col-md-4 mb-4">
                    <h5>Quick Links</h5>
                    <ul class="list-unstyled">
                        <li><a href="/" class="text-white text-decoration-none">Home</a></li>
                        <li><a href="/about" class="text-white text-decoration-none">About</a></li>
                        <li><a href="/products" class="text-white text-decoration-none">Products</a></li>
                        <li><a href="/portfolio" class="text-white text-decoration-none">Portfolio</a></li>
                        <li><a href="/contact" class="text-white text-decoration-none">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-4">
                    <h5>Follow Us</h5>
                    <div class="footer-social">
                        <% if (typeof settings !== 'undefined') { %>
                            <% if (settings.facebook) { %>
                                <a href="<%= settings.facebook %>" target="_blank" rel="noopener"><i class="bi bi-facebook"></i></a>
                            <% } %>
                            <% if (settings.instagram) { %>
                                <a href="<%= settings.instagram %>" target="_blank" rel="noopener"><i class="bi bi-instagram"></i></a>
                            <% } %>
                            <% if (settings.twitter) { %>
                                <a href="<%= settings.twitter %>" target="_blank" rel="noopener"><i class="bi bi-twitter"></i></a>
                            <% } %>
                            <% if (settings.linkedin) { %>
                                <a href="<%= settings.linkedin %>" target="_blank" rel="noopener"><i class="bi bi-linkedin"></i></a>
                            <% } %>
                            <% if (settings.youtube) { %>
                                <a href="<%= settings.youtube %>" target="_blank" rel="noopener"><i class="bi bi-youtube"></i></a>
                            <% } %>
                        <% } %>
                    </div>
                </div>
            </div>
            <hr class="bg-white">
            <div class="text-center" id="footerCopyright">
                <p>&copy; <%= new Date().getFullYear() %> Pool N Play. All rights reserved.</p>
                <p class="mb-0">
                    <a href="/admin/login" class="text-white text-decoration-none">
                        <i class="bi bi-shield-lock"></i> Admin Login
                    </a>
                </p>
            </div>
        </div>
    </footer>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Form submission handler and affiliate text insertion
        document.addEventListener('DOMContentLoaded', function() {
            // Insert affiliate text in footer
            const footerCopyright = document.getElementById('footerCopyright');
            if (footerCopyright) {
                const affiliateText = document.createElement('p');
                affiliateText.className = 'mb-2 text-white-50';
                affiliateText.style.fontSize = '0.9rem';
                affiliateText.innerHTML = 'Affiliate of <strong>Aquarian Pool and Spa</strong>';
                footerCopyright.insertBefore(affiliateText, footerCopyright.querySelector('p:last-of-type'));
            }
            
            // Form submission handler
            const forms = document.querySelectorAll('.inquiry-form');
            forms.forEach(form => {
                form.addEventListener('submit', async function(e) {
                    e.preventDefault();
                    
                    const submitBtn = form.querySelector('button[type="submit"]');
                    const originalText = submitBtn.textContent;
                    submitBtn.disabled = true;
                    submitBtn.textContent = 'Submitting...';
                    
                    const formData = new FormData(form);
                    const data = Object.fromEntries(formData);
                    
                    // Handle multi-select for pool sizes
                    if (form.querySelector('select[name="poolSizes"]')) {
                        const select = form.querySelector('select[name="poolSizes"]');
                        const selected = Array.from(select.selectedOptions).map(opt => opt.value);
                        data.poolSizes = selected;
                    }
                    
                    try {
                        const response = await fetch('/inquiry', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json',
                            },
                            body: JSON.stringify(data),
                        });
                        
                        const result = await response.json();
                        
                        if (result.success) {
                            alert(result.message);
                            form.reset();
                        } else {
                            alert(result.message || 'An error occurred. Please try again.');
                        }
                    } catch (error) {
                        console.error('Error:', error);
                        alert('An error occurred. Please try again.');
                    } finally {
                        submitBtn.disabled = false;
                        submitBtn.textContent = originalText;
                    }
                });
            });
        });
    </script>
</body>
</html>

